# Chan-ta-net

* re-build project
    ```bash
    docker compose down && docker compose build --no-cache && docker compose up -d
    ```

* run project
    ```bash
    docker compose up -d
    ```

* down project
    ```bash
    docker compose down
    ```