# Chan-ta-net

* re-build project
    ```bash
    docker compose up -d --build
    ```

* run project
    ```bash
    docker compose up -d
    ```

* down project
    ```bash
    docker compose down
    ```